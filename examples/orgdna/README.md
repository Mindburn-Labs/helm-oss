# OrgDNA Examples

This directory provides sample OrgDNA policy payloads for testing and demos:

- `finance_approval.json`
- `saas_support.json`

Use these files with the OrgDNA CLI flows to validate synthesis, policy loading,
and conformance behavior.
