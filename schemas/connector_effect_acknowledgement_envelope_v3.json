{
  "$comment": "quantum_posture: the envelope carries a detached classical Ed25519 signature; this schema documents that signing shape and makes no hybrid or post-quantum claim.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helm.mindburn.org/schemas/connector_effect_acknowledgement_envelope_v3.json",
  "title": "Connector Effect Acknowledgement Envelope V3",
  "description": "Detached Ed25519 signature over one sealed connector effect acknowledgement v3 with exact activation, adapter capability, finality bindings, and the exact disposition receipt hash when present.",
  "type": "object",
  "required": ["acknowledgement", "signature"],
  "properties": {
    "acknowledgement": { "$ref": "connector_effect_acknowledgement_v3.json" },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "additionalProperties": false
}
