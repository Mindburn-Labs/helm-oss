{
  "$comment": "quantum_posture: the envelope carries a detached classical Ed25519 signature; this schema documents that signing shape and makes no hybrid or post-quantum claim.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helm.mindburn.org/schemas/connector_effect_acknowledgement_envelope_v2.json",
  "title": "Connector Effect Acknowledgement Envelope V2",
  "description": "Detached Ed25519 signature over one sealed connector effect acknowledgement v2 with exact activation, adapter capability, and finality bindings.",
  "type": "object",
  "required": ["acknowledgement", "signature"],
  "properties": {
    "acknowledgement": { "$ref": "connector_effect_acknowledgement_v2.json" },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "additionalProperties": false
}
