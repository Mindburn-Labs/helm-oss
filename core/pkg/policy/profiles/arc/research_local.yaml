profile_id: "profile.arc-research-local.v1"
description: "Research Harness Policy for local exploratory looping"
mutation_frozen: false
registry:
  allowed_actions:
    - "helm.connector.arc.session.open.v1"
    - "helm.connector.arc.session.step.v1"
limits:
  max_budget: 100
contamination_rules:
  allow_external_http: false
  allow_file_system_writes: false
