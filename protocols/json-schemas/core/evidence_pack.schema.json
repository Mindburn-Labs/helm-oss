{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helm.org/schemas/core/evidence_pack.schema.json",
  "title": "EvidencePack",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "pack_id",
    "format_version",
    "created_at",
    "identity",
    "policy",
    "effect",
    "context",
    "execution",
    "receipts",
    "reconciliation",
    "attestation"
  ],
  "properties": {
    "pack_id": { "type": "string" },
    "format_version": { "type": "string" },
    "created_at": { "type": "string", "format": "date-time" },
    "correlation_id": { "type": "string", "description": "Product request identity (X-Helm-Correlation-ID) joining this pack to its decision, receipts, and lifecycle events. Optional; outside the pack signature until HELM-303." },
    "identity": { "$ref": "#/$defs/identity" },
    "policy": { "$ref": "#/$defs/policy" },
    "effect": { "$ref": "#/$defs/effect" },
    "context": { "$ref": "#/$defs/context" },
    "execution": { "$ref": "#/$defs/execution" },
    "receipts": { "$ref": "#/$defs/receipts" },
    "reconciliation": { "$ref": "#/$defs/reconciliation" },
    "verification_scopes": { "type": "array", "items": { "$ref": "#/$defs/verification_scope" } },
    "harness_trace_refs": { "type": "array", "items": { "$ref": "#/$defs/harness_trace_ref" } },
    "lineage": { "$ref": "#/$defs/lineage" },
    "attestation": { "$ref": "#/$defs/attestation" }
  },
  "$defs": {
    "lineage": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "tenant_id",
        "company_id",
        "workspace_id",
        "environment_id",
        "activation_record_ref",
        "activation_record_hash",
        "outcome_contract_ref",
        "outcome_contract_hash",
        "measurement_plan_ref",
        "measurement_plan_hash",
        "window_identity"
      ],
      "properties": {
        "schema_version": { "const": "evidence-pack-lineage.v1" },
        "tenant_id": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "company_id": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "workspace_id": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "environment_id": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "activation_record_ref": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "activation_record_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        "outcome_contract_ref": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "outcome_contract_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        "measurement_plan_ref": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" },
        "measurement_plan_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        "window_identity": { "type": "string", "minLength": 1, "maxLength": 512, "pattern": "^\\S+$" }
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["actor_id", "actor_type"],
      "properties": {
        "actor_id": { "type": "string" },
        "actor_type": { "type": "string" },
        "session_id": { "type": "string" },
        "delegation_chain": { "type": "array", "items": { "type": "string" } }
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision_id", "policy_version", "rules_fired", "evaluation_graph_hash"],
      "properties": {
        "decision_id": { "type": "string" },
        "policy_version": { "type": "string" },
        "rules_fired": { "type": "array", "items": { "type": "string" } },
        "evaluation_graph_hash": { "type": "string" }
      }
    },
    "effect": {
      "type": "object",
      "additionalProperties": false,
      "required": ["effect_id", "effect_type", "effect_payload_hash"],
      "properties": {
        "effect_id": { "type": "string" },
        "effect_type": { "type": "string" },
        "effect_payload_hash": { "type": "string" },
        "idempotency_key": { "type": "string" },
        "classification": { "type": "string" }
      }
    },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "mode_id": { "type": "string" },
        "loop_id": { "type": "string" },
        "jurisdiction": { "type": "string" },
        "phenotype_hash": { "type": "string" },
        "orchestration_run_id": { "type": "string" },
        "phase_id": { "type": "string" },
        "checkpoint_ref": { "type": "string" },
        "critique_ref": { "type": "string" },
        "heuristic_trace_id": { "type": "string" }
      }
    },
    "execution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["execution_id", "status", "retry_count", "started_at"],
      "properties": {
        "execution_id": { "type": "string" },
        "status": { "type": "string" },
        "result_hash": { "type": "string" },
        "retry_count": { "type": "integer" },
        "started_at": { "type": "string", "format": "date-time" },
        "completed_at": { "type": "string", "format": "date-time" },
        "duration_ms": { "type": "integer" },
        "failure_reason": { "type": "string" }
      }
    },
    "receipts": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "pal_receipts": { "type": "array", "items": { "$ref": "#/$defs/pal_receipt" } },
        "external_receipts": { "type": "array", "items": { "$ref": "#/$defs/external_receipt" } }
      }
    },
    "reconciliation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "reconciliation_id": { "type": "string" },
        "outbox_id": { "type": "string" },
        "compensation_ref": { "type": "string" },
        "denied_attempts": { "type": "array", "items": { "$ref": "#/$defs/denied_attempt" } },
        "failed_attempts": { "type": "array", "items": { "$ref": "#/$defs/failed_attempt" } }
      }
    },
    "attestation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pack_hash"],
      "properties": {
        "pack_hash": { "type": "string" },
        "signature": { "type": "string" },
        "signer_id": { "type": "string" },
        "kernel_version": { "type": "string" }
      }
    },
    "verification_scope": {
      "type": "object",
      "additionalProperties": false,
      "required": ["verification_scope_id", "subject_hash", "checks_performed", "verifier_hash", "policy_hash"],
      "properties": {
        "verification_scope_id": { "type": "string" },
        "subject_hash": { "type": "string" },
        "risk_class": { "type": "string", "enum": ["T0", "T1", "T2", "T3"] },
        "checks_performed": { "type": "array", "items": { "type": "string" } },
        "assumptions": { "type": "array", "items": { "type": "string" } },
        "untested_regions": { "type": "array", "items": { "type": "string" } },
        "known_limits": { "type": "array", "items": { "type": "string" } },
        "remaining_risks": { "type": "array", "items": { "type": "string" } },
        "required_followup": { "type": "array", "items": { "type": "string" } },
        "verifier_hash": { "type": "string" },
        "policy_hash": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" },
        "scope_hash": { "type": "string" }
      }
    },
    "harness_trace_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["trace_id", "hash"],
      "properties": {
        "trace_id": { "type": "string" },
        "hash": { "type": "string" },
        "uri": { "type": "string" },
        "kind": { "type": "string" },
        "at": { "type": "string", "format": "date-time" }
      }
    },
    "pal_receipt": {
      "type": "object",
      "additionalProperties": false,
      "required": ["receipt_id", "provider_id", "input_hash", "output_hash", "completed_at"],
      "properties": {
        "receipt_id": { "type": "string" },
        "provider_id": { "type": "string" },
        "model_id": { "type": "string" },
        "input_hash": { "type": "string" },
        "output_hash": { "type": "string" },
        "tokens_in": { "type": "integer" },
        "tokens_out": { "type": "integer" },
        "completed_at": { "type": "string", "format": "date-time" }
      }
    },
    "external_receipt": {
      "type": "object",
      "additionalProperties": false,
      "required": ["receipt_id", "system_name", "request_hash", "response_hash", "completed_at"],
      "properties": {
        "receipt_id": { "type": "string" },
        "external_id": { "type": "string" },
        "system_name": { "type": "string" },
        "request_hash": { "type": "string" },
        "response_hash": { "type": "string" },
        "http_status": { "type": "integer" },
        "completed_at": { "type": "string", "format": "date-time" }
      }
    },
    "denied_attempt": {
      "type": "object",
      "additionalProperties": false,
      "required": ["attempt_id", "decision_id", "reason", "occurred_at"],
      "properties": {
        "attempt_id": { "type": "string" },
        "decision_id": { "type": "string" },
        "reason": { "type": "string" },
        "occurred_at": { "type": "string", "format": "date-time" }
      }
    },
    "failed_attempt": {
      "type": "object",
      "additionalProperties": false,
      "required": ["attempt_id", "reason", "retry_number", "occurred_at"],
      "properties": {
        "attempt_id": { "type": "string" },
        "reason": { "type": "string" },
        "retry_number": { "type": "integer" },
        "occurred_at": { "type": "string", "format": "date-time" }
      }
    }
  }
}
